# Perturbation protocol

The fake environment relocates the target from `countertop 1` to `pantry 1` at a reproducible checkpoint and records an observable reassessment message. This does not establish an ALFWorld mutation API. Real perturbations require pilot-observed capability and a frozen solvability method.

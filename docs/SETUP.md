# Setup order

1. Create a Python 3.11 environment and install this local package.
2. Run `preflight`, `doctor`, and `validate-config`.
3. Only after explicit approval, use capability probes before installing or configuring external software.
4. Run the full pilot; freeze versions only after a pass report.

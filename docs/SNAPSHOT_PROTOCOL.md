# Snapshot protocol

Create nested, hashed, non-overwritable snapshots from acquisition creation order: `L0` plus provisional proportional snapshots. Validate counts, source metadata, content hashes, and read-only evaluation protection.

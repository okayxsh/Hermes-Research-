# University runbook

Generate a deterministic evaluation queue, split it into machine-specific shards, and merge registries afterward. Record machine IDs/manifests and validate unique run IDs. Start with mock mode and the pilot; do not assume GPU capacity or local model compatibility.

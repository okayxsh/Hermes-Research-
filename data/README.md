# Data policy

Real task lists are intentionally empty until discovered from an installed ALFWorld dataset and frozen through `rq1 tasks`. Do not manually type task IDs, commit ALFWorld data, or use `valid_unseen` during pilot calibration. `pilot_fake.json` is simulated-only and can never be promoted to a real manifest.
